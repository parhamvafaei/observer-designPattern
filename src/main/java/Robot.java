
//using factory method design pattern , the reason is we have two classes with same behavior and treat  therfore create a abstract class and implement the robot classes of it. 

public interface Robot {
    public void  update(Object o);
}
